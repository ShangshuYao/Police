# 采购管理系统 · 维护文档

> 本文档说明重构后的项目结构、各模块职责、加载顺序、依赖关系及维护要点，供后续开发与维护参考。

---

## 一、项目概览

采购管理系统是一个**纯前端单页应用**，使用内嵌 SQLite（sql.js / WebAssembly）作为数据存储，数据持久化到浏览器 IndexedDB。支持两种角色：

- **普通用户**：浏览物品、加入采购车、提交采购申请、查看申请状态
- **管理员**：数据概览、物品/分类管理、审核申请、导出采购明细、用户管理

**技术栈**：原生 HTML + CSS + JavaScript（无框架）、sql.js（SQLite WASM）、IndexedDB 持久化

---

## 二、目录结构

```
Procurement/
├── index.html                    # 应用入口（HTML 骨架 + 模块引入）
├── 阿里云部署教程.md              # 云服务器部署教程（可选）
└── 项目维护文档.md                # 本文档
├── css/
│   └── style.css                 # 全部样式（单文件，按区块注释划分）
└── js/
    ├── vendor/
    │   └── sql-wasm.js           # SQLite 引擎（sql.js，提供全局 initSqlJs）
    ├── utils.js                  # 通用工具函数
    ├── db.js                     # 数据库层（含 WASM base64、schema、查询封装、持久化）
    ├── ui.js                     # 通用 UI 组件（弹窗、Toast）
    ├── pages/                    # 各页面模块
    │   ├── shop.js               # 普通用户：采购大厅 + 采购车
    │   ├── my-requests.js        # 普通用户：我的申请
    │   ├── dashboard.js          # 管理员：数据概览 + 数据库导入/导出
    │   ├── items.js              # 管理员：物品管理 + 分类管理
    │   ├── review.js             # 管理员：审核中心
    │   ├── export.js             # 管理员：采购明细 + CSV 导出
    │   └── users.js              # 管理员：用户管理
    └── app.js                    # 应用入口（会话管理、路由、启动流程）
```

---

## 三、加载顺序与依赖关系

### 3.1 加载顺序（index.html 中 `<script>` 标签顺序）

```
1. css/style.css                         ← 样式
2. js/vendor/sql-wasm.js                 ← 提供全局函数 initSqlJs
3. js/utils.js                           ← 通用工具
4. js/db.js                              ← 数据库层（依赖 utils.js 的 b64ToBytes/bytesToB64）
5. js/ui.js                              ← 弹窗/Toast（依赖 utils.js 的 esc）
6. js/pages/shop.js                      ← 依赖 db.js、ui.js、utils.js
7. js/pages/my-requests.js               ← 依赖 db.js、utils.js
8. js/pages/dashboard.js                 ← 依赖 db.js、ui.js、utils.js
9. js/pages/items.js                     ← 依赖 db.js、ui.js、utils.js
10. js/pages/review.js                   ← 依赖 db.js、ui.js、utils.js
11. js/pages/export.js                   ← 依赖 db.js、utils.js
12. js/pages/users.js                    ← 依赖 db.js、ui.js、utils.js
13. js/app.js                            ← 启动入口，依赖以上所有模块
```

### 3.2 依赖关系图

```
sql-wasm.js ──► db.js ──┬──► pages/shop.js
                        ├──► pages/my-requests.js
                        ├──► pages/dashboard.js
                        ├──► pages/items.js
                        ├──► pages/review.js
                        ├──► pages/export.js
                        └──► pages/users.js
                              │
utils.js ──┬───────────────► 全部 pages
           └──► ui.js
                              │
                              └──► app.js（整合所有，启动应用）
```

**关键约束**：
- `db.js` 必须在 `sql-wasm.js` 之后加载（`initDb()` 调用 `initSqlJs`）
- `ui.js` 必须在 `utils.js` 之后（`showModal` 使用 `esc()`）
- `app.js` 必须最后加载（调用各页面的 `viewXxx()` 函数）

---

## 四、各模块职责详解

### 4.1 `index.html` — 应用入口

**职责**：只提供 HTML 骨架结构，不包含任何业务逻辑。通过 `<link>` 和 `<script>` 按顺序引入所有模块。

**包含的 DOM 节点**：
- `#boot` — 启动遮罩（WASM 初始化时显示）
- `#loginPage` — 登录页
- `#app` — 主应用容器（header + sidebar + main）
- `#modalOverlay` — 弹窗容器
- `#toast` — 提示条

### 4.2 `css/style.css` — 样式

**职责**：所有样式集中管理，按功能区块用注释分隔：
- 基础重置 / 按钮 / 输入框 / 表格 / 卡片
- 启动遮罩 `#boot`
- 登录页 `#loginPage`
- 布局（header / aside / main）
- 采购大厅（`.shop-bar` `.cat-tab` `.goods`）
- 统计卡片（`.stat-grid` `.stat`）
- 状态徽章（`.badge`）
- 工具条 / 分类标签
- 弹窗 / Toast

### 4.3 `js/vendor/sql-wasm.js` — SQLite 引擎

**职责**：sql.js 编译产物，提供全局函数 `initSqlJs(config)`，返回 Promise，resolve 为 SQL 模块对象（含 `Database` 构造器）。

**来源**：从 `https://cdn.jsdelivr.net/npm/sql.js@1.13.0/dist/sql-wasm.js` 下载。

**维护提示**：如需升级 sql.js，用新版替换本文件，同时确认 API 兼容。

### 4.4 `js/utils.js` — 通用工具

**职责**：纯函数，无副作用，无 DOM 依赖。

| 函数 | 说明 |
|---|---|
| `genId(prefix)` | 生成唯一 ID（前缀 + 时间戳 + 随机串） |
| `fmt(n)` | 数字格式化为两位小数 |
| `esc(s)` | HTML 转义，防 XSS |
| `nowStr()` | 当前时间字符串 `YYYY-MM-DD HH:mm` |
| `val(id)` | 获取输入框值 |
| `b64ToBytes(b64)` / `bytesToB64(bytes)` | base64 与 Uint8Array 互转 |
| `csvCell(v)` | CSV 单元格转义 |
| `safeFileName(s)` | 文件名非法字符替换 |

### 4.5 `js/db.js` — 数据库层

**职责**：SQLite 引擎初始化、表结构、种子数据、持久化、查询封装。

**包含内容**：
- `SQL_WASM_B64` — wasm 二进制的 base64 编码（内联，约 880KB）
- `SQL`, `sqdb`, `idb` — 全局数据库句柄
- `SCHEMA` — 5 张表的建表语句

```
users(username PK, password, role)
categories(id PK, name UNIQUE)
items(id PK, name, cat_id, price, unit)
requests(id PK, no UNIQUE, username, date, status, note, comment, review_time)
request_items(id PK AUTO, request_id, item_id, name, cat_name, price, unit, qty)
```

**核心函数**：

| 函数 | 说明 |
|---|---|
| `q(sql, params)` | 执行查询，返回对象数组 |
| `one(sql, params)` | 执行查询，返回首行或 undefined |
| `run(sql, params)` | 执行写操作 |
| `tx(fn)` | 事务包裹，成功 COMMIT 失败 ROLLBACK，最后持久化 |
| `seedDatabase()` | 初始化默认数据（admin/user 账号、2 分类、16 物品、1 示例申请） |
| `migrateLegacy()` | 旧版 localStorage 数据迁移（向后兼容） |
| `persist()` | 将数据库导出为二进制，写入 IndexedDB（失败降级 localStorage） |
| `initDb()` | 异步加载 WASM → 恢复已有数据 或 初始化种子 → 持久化 |
| `itemById(id)` / `allItems()` / `loadRequests(username)` | 常用查询封装 |
| `statusText(s)` / `statusBadge(s)` / `reqTotal(r)` | 展示辅助函数 |

**维护提示**：
- 新增表需在 `SCHEMA` 数组中添加 `CREATE TABLE`
- 如需修改种子数据，编辑 `seedDatabase()`
- 持久化失败时会调用 `toast()`，所以 `ui.js` 必须在运行时可用（加载顺序已保证）

### 4.6 `js/ui.js` — 通用 UI 组件

**职责**：弹窗和提示条。

| 函数 | 说明 |
|---|---|
| `showModal(title, bodyHtml, btns)` | 显示弹窗，btns 为 `[css类, 按钮文字, onclick代码]` 数组 |
| `closeModal()` | 关闭弹窗 |
| `toast(msg)` | 显示 2.5 秒提示条 |

### 4.7 `js/pages/shop.js` — 采购大厅（普通用户）

**职责**：物品浏览、分类筛选、搜索、加入采购车、采购车确认、提交申请。

**状态变量**：`shopCat`, `shopKw`

**关键函数**：`viewShop()`, `addToCart()`, `openCartModal()`, `submitRequest()`

### 4.8 `js/pages/my-requests.js` — 我的申请（普通用户）

**职责**：展示当前用户的所有采购申请及审核结果。

**关键函数**：`viewMyRequests()`, `requestCard(r)`, `reqItemsTable(r)`

### 4.9 `js/pages/dashboard.js` — 数据概览（管理员）

**职责**：统计卡片、待审核申请列表、数据库导入/导出。

**关键函数**：`viewDashboard()`, `exportDb()`, `importDb(input)`

### 4.10 `js/pages/items.js` — 物品管理（管理员）

**职责**：分类增删、物品增删改。

**关键函数**：`viewItems()`, `addCategory()`, `delCategory(id)`, `addItem()`, `delItem(id)`, `openItemModal(id)`, `saveItem(id)`

### 4.11 `js/pages/review.js` — 审核中心（管理员）

**职责**：按状态筛选申请、通过/驳回（驳回必填意见）。

**状态变量**：`reviewFilter`

**关键函数**：`viewReview()`, `openReviewModal(id, action)`, `doReview(id, action)`

### 4.12 `js/pages/export.js` — 采购明细（管理员）

**职责**：明细列表、状态/关键词筛选、CSV 导出（合并 / 按申请人 / 按单号）。

**状态变量**：`exportFilter`, `exportKw`, `exportMode`

**关键函数**：`viewExport()`, `exportRows()`, `exportCSV()`, `csvContent(rows)`, `downloadCSV(filename, content)`

### 4.13 `js/pages/users.js` — 用户管理（管理员）

**职责**：添加/删除普通用户（admin 受保护）。

**关键函数**：`viewUsers()`, `addUser()`, `delUser(name)`

### 4.14 `js/app.js` — 应用入口

**职责**：会话管理、页面路由、启动流程。

**全局状态**：`currentUser`, `view`, `cart`

**关键函数**：

| 函数 | 说明 |
|---|---|
| `doLogin()` | 校验账号密码，建立会话，进入应用 |
| `logout()` | 清除会话，返回登录页 |
| `enterApp()` | 渲染用户信息、导航栏、当前页面 |
| `switchView(v)` | 切换页面 |
| `render()` | 路由分发，调用对应 `viewXxx()` |
| `renderSidebar()` | 根据角色渲染侧边栏 |
| `loadCart()` / `saveCart()` | 采购车 localStorage 持久化 |
| `boot()`（IIFE） | 启动入口：`initDb()` → 恢复会话 → 进入应用或登录页 |

---

## 五、页面路由表

`app.js` 的 `render()` 函数根据 `view` 变量分发到各页面：

| view 值 | 角色 | 渲染函数 | 所在模块 |
|---|---|---|---|
| `shop` | 普通用户 | `viewShop()` | pages/shop.js |
| `myreq` | 普通用户 | `viewMyRequests()` | pages/my-requests.js |
| `dashboard` | 管理员 | `viewDashboard()` | pages/dashboard.js |
| `items` | 管理员 | `viewItems()` | pages/items.js |
| `review` | 管理员 | `viewReview()` | pages/review.js |
| `export` | 管理员 | `viewExport()` | pages/export.js |
| `users` | 管理员 | `viewUsers()` | pages/users.js |

**导航渲染**：`renderSidebar()` 根据 `currentUser.role` 决定显示哪些菜单项。

---

## 六、数据流概览

```
浏览器加载 index.html
    │
    ▼
按顺序加载 JS 模块
    │
    ▼
app.js 的 boot() 执行
    │
    ├──► initDb()  (db.js)
    │       ├── 加载 WASM (sql-wasm.js)
    │       ├── 从 IndexedDB 恢复 或 初始化种子数据
    │       └── persist() 持久化
    │
    ├──► 检查 localStorage 中的会话
    │       ├── 有有效会话 → enterApp()
    │       └── 无会话 → 显示登录页
    │
    ▼
用户操作 → 各页面函数 → q()/run()/tx() → sqdb
                                          │
                                          ▼
                                    persist() → IndexedDB
```

---

## 七、维护要点

### 7.1 新增物品字段

1. 在 `db.js` 的 `SCHEMA` 中修改 `items` 表定义
2. 修改 `seedDatabase()` 中的物品数据
3. 修改 `items.js` 的 `viewItems()` / `addItem()` / `saveItem()` 表单与 SQL
4. 修改 `shop.js` 的 `goodsGrid()` 显示

### 7.2 新增页面

1. 在 `js/pages/` 下创建 `xxx.js`，实现 `viewXxx()` 函数
2. 在 `app.js` 的 `render()` 中添加 `else if(view==='xxx') main.innerHTML = viewXxx();`
3. 在 `renderSidebar()` 中添加导航项
4. 在 `index.html` 中按顺序添加 `<script src="js/pages/xxx.js"></script>`（必须在 app.js 之前）

### 7.3 修改默认账号/密码

编辑 `db.js` 的 `seedDatabase()` 中 `INSERT INTO users` 语句。

**注意**：已存在数据库的浏览器不会重新执行种子数据，需清除 IndexedDB 或用导入功能覆盖。

### 7.4 数据持久化位置

- 优先：IndexedDB 数据库 `pm_procurement` 的 `kv` 存储，key 为 `db`，值为 SQLite 二进制
- 降级：localStorage 的 `pm_sqlite_b64`，值为 base64 编码
- 采购车：localStorage 的 `pm_cart_{username}`

清除浏览器数据会重置为种子数据。

### 7.5 数据库迁移

- 数据概览页提供「导出 .db」和「导入 .db」功能，用于跨浏览器/设备迁移
- 导入时会校验数据库必须包含至少一个管理员账号

---

## 八、常见问题排查

| 现象 | 原因 | 解决 |
|---|---|---|
| 启动遮罩一直转 | WASM 加载失败 | 检查 js/vendor/sql-wasm.js 和 js/db.js 是否正确上传 |
| 登录后白屏 | 某页面模块加载顺序错误或缺依赖 | 检查浏览器 Console 报错，核对 index.html 的 script 顺序 |
| 数据丢失/重置 | 清除了浏览器存储 | 无法恢复，需重新录入；建议定期导出 .db 备份 |
| 新功能不生效 | 浏览器缓存旧 JS | 强制刷新（Ctrl+F5）或给静态资源加版本号 |
| `esc is not defined` | utils.js 在 ui.js 之后加载 | 调整 script 顺序，utils.js 必须在 ui.js 之前 |
| `initSqlJs is not defined` | sql-wasm.js 未加载 | 确认 vendor/sql-wasm.js 路径正确 |

---

## 九、版本记录

| 日期 | 变更 |
|---|---|
| 2026-09-06 | 项目重构：单文件 index.html 拆分为模块化结构（css/ + js/pages/），功能不变 |
