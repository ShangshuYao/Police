# 采购管理系统 · 阿里云服务器部署教程

> 目标：把采购管理系统（Flask + SQLite）部署到阿里云服务器，让所有设备通过域名访问同一个服务端，实现数据实时互通。
>
> 架构示意：
> ```
> 多台设备浏览器 ──► 你的域名/服务器IP ──► 阿里云服务器（Flask + procurement.db）
> ```

---

## 目录

1. [购买云服务器](#一购买云服务器)
2. [配置防火墙（安全组）](#二配置防火墙安全组)
3. [首次连接服务器](#三首次连接服务器)
4. [安装 Python 环境](#四安装-python-环境)
5. [上传项目文件](#五上传项目文件)
6. [首次启动测试](#六首次启动测试)
7. [配置开机自启（systemd）](#七配置开机自启systemd)
8. [安装 nginx 并绑定 80 端口（推荐）](#八安装-nginx-并绑定-80-端口推荐)
9. [域名解析与备案](#九域名解析与备案)
10. [日常运维（备份 / 更新 / 日志）](#十日常运维)
11. [常见问题排查](#十一常见问题排查)
12. [下一步](#十二下一步)

---

## 〇、开始前的准备

- 一台能上网的 Windows 电脑（你现在这台即可）
- 一个阿里云账号（https://www.aliyun.com 注册，需实名认证）
- 你的虚拟域名（若域名打算指向大陆服务器，需提前准备备案材料，见第九步）

---

## 一、购买云服务器

阿里云有两类产品都适合，**新手推荐轻量应用服务器**（面板简单，防火墙在控制台点几下即可）：

| 产品 | 特点 | 入口 |
|---|---|---|
| 轻量应用服务器 | 一站式面板、流量套餐、配置简单 | https://www.aliyun.com/product/swas |
| ECS 云服务器 | 功能全面、需自行配置安全组 | https://www.aliyun.com/product/ecs |

### 选型建议（本系统非常轻量，无需高配）

| 项目 | 建议 | 说明 |
|---|---|---|
| **地域** | 国内用户为主 → 华东（杭州）/华北（北京）；**域名未备案 → 选「中国香港」** | 大陆地域绑定域名对外提供网站必须 ICP 备案；香港地域免备案，延迟略高但完全可用 |
| **镜像/系统** | Ubuntu 22.04 64 位 | 本教程所有命令以 Ubuntu 22.04 为准 |
| **配置** | 2 核 2G 起步 | 几十人同时使用毫无压力 |
| **带宽** | 3~5M 固定带宽，或按量计费 | 系统页面小，3M 足够 |
| **磁盘** | 默认 40~60G ESSD | 默认即可 |

### 价格参考

轻量应用服务器 2核2G 新用户活动价常见 **¥50~200/年**，续费原价约 ¥300~600/年；以官网实时价格为准，**新用户/学生有优惠，付款前先看活动页**。

### 购买时注意

- 计费模式选「包年包月」（长期用更划算）
- 设置 **root 密码**（或购买后在控制台「重置实例密码」），**务必记住**
- 购买完成后进入控制台，确认实例状态为「运行中」，并记下**公网 IP**

---

## 二、配置防火墙（安全组）

需要在云端放行以下端口，否则外部无法访问：

| 端口 | 用途 | 是否必须 |
|---|---|---|
| 22 | SSH 远程连接 | 必须（一般默认已放行） |
| 80 | HTTP 网站访问（nginx） | 推荐 |
| 443 | HTTPS 访问 | 可选（上证书时用） |
| 8000 | Flask 直连端口 | 测试阶段临时用，配好 nginx 后可关掉 |

### 轻量应用服务器

控制台 → 进入实例详情 → 「防火墙」标签 → 添加规则：
- 应用类型选「自定义」，协议 TCP，端口范围分别填 `80`、`443`、`8000`，授权对象 `0.0.0.0/0`

### ECS 云服务器

控制台 → 实例列表 → 点击实例 → 「安全组」标签 → 配置规则 → 「入方向」→ 手动添加：
- 优先级 1，授权策略「允许」，协议类型「自定义 TCP」，端口范围 `80/80`、`443/443`、`8000/8000`，授权对象 `0.0.0.0/0`

> ✅ 检查点：规则列表中能看到上述端口均为「允许」状态。

---

## 三、首次连接服务器

### 方式 A：浏览器终端 Workbench（推荐，零配置）

1. 阿里云控制台 → 实例列表 → 点击对应实例的「远程连接」
2. 选择「Workbench 远程连接」→ 输入用户名 `root` 和你购买时设置的密码
3. 浏览器内出现黑色终端窗口即连接成功

### 方式 B：本地 PowerShell SSH

Windows 10/11 自带 SSH 客户端，直接打开 PowerShell：

```powershell
ssh root@服务器公网IP
# 首次连接输入 yes，然后输入 root 密码
```

> ✅ 检查点：终端提示符变成 `root@iZxxxxx:~#` 样式，即登录成功。以下命令均在**服务器终端**执行。

---

## 四、安装 Python 环境

```bash
# 1. 更新软件源
apt update -y

# 2. 安装 Python3、pip、虚拟环境模块（Ubuntu 22.04 自带 Python 3.10）
apt install -y python3 python3-pip python3-venv

# 3. 确认版本
python3 -V        # 应输出 Python 3.10.x
```

创建项目目录和独立虚拟环境（避免污染系统环境）：

```bash
mkdir -p /opt/procurement && cd /opt/procurement
python3 -m venv venv
source venv/bin/activate

# 安装 Flask 与 gunicorn（使用阿里云镜像源，速度快）
pip install flask gunicorn -i https://mirrors.aliyun.com/pypi/simple/
```

> ✅ 检查点：`flask --version` 能正常输出版本号。
> 注意：每次重新登录服务器后，需先执行 `cd /opt/procurement && source venv/bin/activate` 再操作 pip/python。

---

## 五、上传项目文件

需要上传 **2 个文件**：`app.py`（服务端）和 `index.html`（前端界面）。
（这两个文件的云端版本我会在你服务器买好后帮你生成，你只管上传。）

### 方式 A：PowerShell scp 命令（在 Windows 本机执行）

```powershell
scp D:\Trae\PoliceProject\Procurement\app.py root@服务器公网IP:/opt/procurement/app.py
scp D:\Trae\PoliceProject\Procurement\index.html root@服务器公网IP:/opt/procurement/index.html
```

### 方式 B：WinSCP 图形化拖拽（不想敲命令就用这个）

1. 下载安装 WinSCP：https://winscp.net
2. 新建会话：文件协议 SFTP，主机名=公网 IP，用户名 root，输入密码
3. 左侧本地窗口选中文件，拖到右侧 `/opt/procurement/` 目录

### 服务器上最终目录结构

```
/opt/procurement/
├── venv/            # Python 虚拟环境
├── app.py           # 服务端程序
├── index.html       # 前端界面
└── (procurement.db 首次运行时自动生成)
```

---

## 六、首次启动测试

```bash
cd /opt/procurement
source venv/bin/activate
python3 app.py
```

看到类似 `Running on http://0.0.0.0:8000` 的输出后，在**你自己的电脑浏览器**访问：

```
http://服务器公网IP:8000
```

> ✅ 检查点：能打开采购管理系统登录页，并用 admin/123456 登录成功。
> 测试通过后按 `Ctrl + C` 停止，进入下一步（正式运行方式）。

---

## 七、配置开机自启（systemd）

用 gunicorn 作为生产级运行方式，并设置开机自启、崩溃自动重启：

```bash
nano /etc/systemd/system/procurement.service
```

粘贴以下内容（`Ctrl+O` 回车保存，`Ctrl+X` 退出）：

```ini
[Unit]
Description=Procurement Management System
After=network.target

[Service]
WorkingDirectory=/opt/procurement
ExecStart=/opt/procurement/venv/bin/gunicorn -w 1 -b 127.0.0.1:8000 app:app
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

启动并设置自启：

```bash
systemctl daemon-reload
systemctl enable --now procurement
systemctl status procurement     # 看到 Active: active (running) 即成功
```

> 说明：`-w 1` 表示单进程，是为了配合 SQLite 避免多进程写锁冲突；本系统并发量很小，单进程完全够用。

常用管理命令：

```bash
systemctl restart procurement    # 重启（更新代码后执行）
systemctl stop procurement       # 停止
journalctl -u procurement -f     # 实时查看运行日志（Ctrl+C 退出）
```

---

## 八、安装 nginx 并绑定 80 端口（推荐）

好处：访问 `http://服务器IP` 无需带 `:8000` 端口；以后上 HTTPS 也靠它。

```bash
apt install -y nginx

nano /etc/nginx/sites-available/procurement
```

粘贴以下配置：

```nginx
server {
    listen 80;
    server_name _;   # 绑定域名后改成你的域名，如 pm.example.com

    access_log /var/log/nginx/procurement_access.log;
    client_max_body_size 20m;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

启用配置：

```bash
ln -sf /etc/nginx/sites-available/procurement /etc/nginx/sites-enabled/
rm -f /etc/nginx/sites-enabled/default
nginx -t && systemctl reload nginx
```

> ✅ 检查点：浏览器访问 `http://服务器公网IP`（不带端口）能打开系统。
> 确认无误后，可回到第二步把防火墙中的 8000 端口规则**删除**（不再对外暴露直连端口，更安全）。

---

## 九、域名解析与备案

### 1. ICP 备案（大陆服务器必须，香港服务器跳过）

阿里云控制台 → 搜索「ICP 备案」→ 按引导填写主体与网站信息 → 提交管局审核。
- 个人/企业身份证（或营业执照）、域名证书等
- 全程线上办理，**免费**，一般 1~2 周
- 备案期间网站可先用 `http://公网IP` 访问调试

### 2. 域名解析

阿里云控制台 → 「云解析 DNS」→ 你的域名 → 添加记录：

| 记录类型 | 主机记录 | 记录值 | TTL |
|---|---|---|---|
| A | `@`（或子域前缀如 `pm`） | 服务器公网 IP | 10 分钟 |

### 3. 绑定到 nginx

```bash
nano /etc/nginx/sites-available/procurement
# 把 server_name _; 改为 server_name 你的域名;
nginx -t && systemctl reload nginx
```

### 4.（可选）免费 HTTPS 证书

```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d 你的域名
# 按提示选择重定向到 HTTPS，certbot 会自动配置续期
```

完成后即可通过 `https://你的域名` 访问。

---

## 十、日常运维

### 数据库自动备份（重要！每天凌晨 2 点）

```bash
crontab -e
# 末尾添加一行（保存退出即生效）：
0 2 * * * cp /opt/procurement/procurement.db /opt/procurement/backup_$(date +\%Y\%m\%d).db
```

建议每月把最近的 backup 文件通过 WinSCP 下载到本地一份。

### 更新系统代码

本地改好文件后重新上传覆盖（scp 或 WinSCP），然后在服务器执行：

```bash
systemctl restart procurement
```

### 查看日志

```bash
journalctl -u procurement -n 100          # 应用最近 100 行日志
tail -f /var/log/nginx/procurement_access.log   # 访问日志
```

### 安全建议

- 部署后**第一时间修改 admin / user 的默认密码**
- SSH 密码不要太简单；更稳妥的做法是改用密钥登录并禁用密码登录（需要时我帮你配）
- 8000 端口配好 nginx 后从安全组移除

---

## 十一、常见问题排查

| 现象 | 排查步骤 |
|---|---|
| 浏览器完全打不开 | ① 控制台防火墙/安全组是否放行对应端口；② `systemctl status procurement` 是否 active (running)；③ 服务器上 `curl 127.0.0.1:8000` 是否有响应 |
| 提示 502 Bad Gateway | gunicorn 进程没起来：`journalctl -u procurement -n 50` 看具体报错（常见是 app.py 上传不完整或依赖未装） |
| IP 能访问，域名不行/被拦截 | 大陆服务器绑定域名必须完成 ICP 备案；未备案请换香港地域，或先完成备案 |
| 访问速度慢 | 检查带宽是否跑满（控制台监控）；大陆服务器配未备案域名会被阻断 |
| 数据丢了 | procurement.db 在 `/opt/procurement/` 目录；重装系统/误删目录会清空——务必按第十步配置每日备份 |
| 忘记 root 密码 | 控制台 → 实例 → 「重置实例密码」→ 重启实例生效 |

---

## 十二、下一步

服务器购买并按上述步骤完成环境安装（第一~四步）后，告诉我一声，我会：

1. 重新生成**云端版 `app.py`**（之前的按你要求删除了）：监听 `0.0.0.0`、预置数据初始化、默认密码修改功能等；
2. 把 `index.html` 改回**调用服务端 API** 的版本（替换掉现在的内嵌 SQLite 版）；
3. 你只需按第五步上传这两个文件，再执行第六步测试即可上线。

之后所有设备（电脑/手机）访问 `http://你的域名`，数据实时互通，管理员在任一设备上都能看到所有用户提交的申请。
