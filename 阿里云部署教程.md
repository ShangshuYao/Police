# 采购管理系统 · 阿里云 ECS 部署教程（Ubuntu 22.04）

> **目标**：把采购管理系统部署到阿里云 ECS 服务器，让所有设备通过域名访问同一个服务端，实现数据实时互通。
>
> **架构示意**：
> ```
> 多台设备浏览器 ──► 你的域名/服务器IP ──► 阿里云 ECS（Flask + procurement.db）
> ```
>
> **项目当前结构**（模块化前端 + 后端 API）：
> ```
> Procurement/
> ├── index.html              # 前端入口
> ├── css/style.css           # 样式
> ├── js/                     # 前端逻辑模块
> │   ├── vendor/sql-wasm.js  # SQLite WASM 引擎
> │   ├── utils.js            # 工具函数
> │   ├── db.js               # 数据库层
> │   ├── ui.js               # UI 组件
> │   ├── app.js              # 应用入口
> │   └── pages/             # 各页面模块
> ├── app.py                  # 服务端（Flask REST API，需生成）
> └── 阿里云部署教程.md        # 本文档
> ```

---

## 目录

1. [登录 ECS 控制台并确认实例状态](#一登录-ecs-控制台并确认实例状态)
2. [配置安全组放行端口](#二配置安全组放行端口)
3. [远程连接服务器](#三远程连接服务器)
4. [更新系统并安装基础软件（含 Git）](#四更新系统并安装基础软件含-git)
5. [安装 Python 环境与依赖](#五安装-python-环境与依赖)
6. [本地推送项目到 GitHub](#六本地推送项目到-github)
7. [服务器从 GitHub 克隆项目](#七服务器从-github-克隆项目)
8. [生成服务端 app.py](#八生成服务端-apppy)
9. [首次启动测试](#九首次启动测试)
10. [配置 gunicorn 生产运行](#十配置-gunicorn-生产运行)
11. [配置 systemd 开机自启](#十一配置-systemd-开机自启)
12. [安装 nginx 反向代理](#十二安装-nginx-反向代理)
13. [域名解析与备案](#十三域名解析与备案)
14. [配置 HTTPS（可选）](#十四配置-https可选)
15. [日常运维（含 GitHub 更新流程）](#十五日常运维含-github-更新流程)
16. [常见问题排查](#十六常见问题排查)
17. [下一步](#十七下一步)

---

## 〇、开始前的准备

### 你需要准备的东西

| 项目 | 说明 |
|---|---|
| 阿里云账号 | 已注册并完成实名认证（https://www.aliyun.com） |
| ECS 实例 | 已购买，系统 Ubuntu 22.04，状态为「运行中」 |
| 公网 IP | 在控制台实例详情页能看到，形如 `8.136.xx.xx` |
| root 密码 | 购买时设置，或购买后在控制台「重置实例密码」获得 |
| 域名 | 若要绑定域名访问，需提前准备（大陆地域必须 ICP 备案） |
| 本地电脑 | Windows 10/11，用于 SSH 连接和上传文件 |
| GitHub 账号 | 免费注册（https://github.com），用于托管项目代码 |

### 整体流程概览

```
配置安全组 → 远程连接 → 装环境(Git/Python)
→ 本地推送 GitHub → 服务器 git clone → 生成 app.py
→ 启动测试 → gunicorn → systemd → nginx → 域名 → 上线
                                        （后续更新：服务器 git pull 即可）
```

---

## 一、登录 ECS 控制台并确认实例状态

### 作用

确认你的服务器已正常运行，获取公网 IP 地址（后续所有操作都需要用到）。

### 步骤

1. 打开浏览器，访问 https://ecs.console.aliyun.com
2. 登录你的阿里云账号
3. 在左侧导航栏点击「**实例与镜像 → 实例**」
4. 找到你的 ECS 实例，确认以下信息：
   - **实例状态**：显示「运行中」✅
   - **实例规格**：如 2核2G（本系统 2核2G 足够）
   - **镜像**：Ubuntu 22.04 64位
   - **公网 IP**：形如 `8.136.198.55`，**记下这个地址**（下面用 `<你的IP>` 代替）

> ⚠️ 如果状态不是「运行中」，点击实例右侧「启动」按钮。

---

## 二、配置安全组放行端口

### 作用

阿里云 ECS 默认有一个安全组（相当于云端防火墙），只放行了 22 端口（SSH）。你网站需要的 80 端口默认是关闭的，外部无法访问。必须手动放行。

### 需要放行的端口

| 端口 | 用途 | 是否必须 |
|---|---|---|
| 22 | SSH 远程连接（默认已放行） | 必须 |
| 80 | HTTP 网站访问（nginx 反代） | **必须** |
| 443 | HTTPS 安全访问 | 可选（配证书时需要） |
| 8000 | Flask 直接访问（仅测试阶段用） | 临时，配好 nginx 后关闭 |

### 步骤（ECS 安全组配置）

1. 在 ECS 控制台实例列表中，**点击你的实例名称**进入详情页
2. 点击顶部「**安全组**」标签页
3. 点击安全组 ID 链接（形如 `sg-xxxxx`），进入安全组详情
4. 点击「**入方向**」→「**手动添加**」
5. 依次添加以下规则（每条都要单独添加）：

   | 规则1 | 规则2 | 规则3 |
   |---|---|---|
   | 授权策略：允许 | 授权策略：允许 | 授权策略：允许 |
   | 协议类型：自定义 TCP | 协议类型：自定义 TCP | 协议类型：自定义 TCP |
   | 端口范围：`80/80` | 端口范围：`443/443` | 端口范围：`8000/8000` |
   | 授权对象：`0.0.0.0/0` | 授权对象：`0.0.0.0/0` | 授权对象：`0.0.0.0/0` |
   | 描述：HTTP | 描述：HTTPS | 描述：Flask测试 |

6. 添加完成后，规则列表应显示 3 条新规则，状态均为「允许」

> ✅ **检查点**：安全组入方向规则列表中能看到 80、443、8000 端口均为「允许」状态。

---

## 三、远程连接服务器

### 作用

通过 SSH 协议远程登录服务器的命令行终端，后续所有安装和配置操作都在此终端中执行。

### 方式 A：阿里云控制台 Workbench（推荐新手，零配置）

1. 在 ECS 控制台实例列表，点击你的实例右侧「**远程连接**」
2. 选择「**Workbench 远程连接**」
3. 输入用户名：`root`
4. 输入密码：你购买时设置的 root 密码
5. 点击「确认」后，浏览器内打开一个黑色终端窗口
6. 看到提示符 `root@iZxxxxx:~#` 即连接成功

### 方式 B：本地 PowerShell SSH（推荐老手，效率更高）

Windows 10/11 自带 SSH 客户端，无需安装额外软件：

1. 按 `Win + R`，输入 `powershell`，回车打开 PowerShell
2. 输入以下命令（把 `<你的IP>` 替换为实际公网 IP）：

```powershell
ssh root@<你的IP>
# 例如：ssh root@8.136.198.55
```

3. 首次连接会提示 `Are you sure you want to continue connecting?`，输入 `yes` 回车
4. 输入 root 密码（输入时屏幕不显示字符，这是正常的），回车
5. 看到提示符 `root@iZxxxxx:~#` 即连接成功

> ✅ **检查点**：终端提示符变为 `root@iZxxxxx:~#`，说明已成功登录服务器。
>
> ⚠️ **以下所有命令均在服务器终端中执行**，除非特别说明「在本地电脑执行」。

---

## 四、更新系统并安装基础软件（含 Git）

### 作用

- 更新系统软件包到最新版本，修复已知安全漏洞
- 安装 `nano`（文本编辑器，用于编辑配置文件）、`wget`（下载工具）、`git`（版本控制，用于从 GitHub 拉取代码）

### 步骤

在服务器终端执行：

```bash
# 1. 更新软件包列表（从阿里云镜像源获取最新软件版本信息）
#    -y 表示自动回答 yes，无需手动确认
apt update -y

# 2. 升级已安装的软件包到最新版本
apt upgrade -y

# 3. 安装 nano 文本编辑器、wget 下载工具、git 版本控制工具
#    git：用于从 GitHub 克隆和更新项目代码
apt install -y nano wget git
```

> **命令解释**：
> - `apt update`：从软件源服务器拉取最新的软件包列表（不安装任何东西，只是更新目录）
> - `apt upgrade`：根据上一步的列表，把已安装的软件升级到最新版本
> - `apt install`：安装指定的新软件包
> - `-y`：自动确认，省去手动输入 y 的步骤

> ✅ **检查点**：
> - `nano --version` 能输出版本号，说明 nano 安装成功
> - `git --version` 能输出版本号（如 git version 2.34.x），说明 git 安装成功

---

## 五、安装 Python 环境与依赖

### 作用

采购管理系统的服务端使用 Python + Flask 框架。Ubuntu 22.04 自带 Python 3.10，但需要额外安装 pip（包管理器）和 venv（虚拟环境模块）。

> **说明**：本章先安装好 Python 基础环境；项目虚拟环境会在第七章从 GitHub 克隆项目后，在项目目录内创建。

### 步骤

```bash
# 1. 安装 Python3、pip（Python 包管理器）、venv（虚拟环境模块）
#    Ubuntu 22.04 已预装 Python 3.10，这里补全 pip 和 venv
apt install -y python3 python3-pip python3-venv

# 2. 确认 Python 版本
python3 -V
# 应输出：Python 3.10.x
```

> **什么是虚拟环境（venv）？**
> 虚拟环境是一个隔离的 Python 运行空间，你在其中安装的包不会影响系统全局环境。这样可以避免不同项目之间的依赖冲突。

> ✅ **检查点**：`python3 -V` 输出 `Python 3.10.x`，`pip3 --version` 输出版本号。

---

## 六、本地推送项目到 GitHub

### 作用

把本地电脑上的项目代码托管到 GitHub 仓库。这样做有两个好处：
1. **代码备份**：代码保存在云端，本地电脑损坏也不丢失
2. **服务器更新方便**：以后项目改了，本地推送到 GitHub，服务器只要 `git pull` 就能更新，不用再用 scp/WinSCP 一个个传文件

### 项目中的两个配套文件

项目根目录已包含：
- **`.gitignore`**：告诉 Git 哪些文件**不要**上传。重要规则：
  - `venv/`（服务器上生成的 Python 环境，几百 MB，无需上传）
  - `*.db`、`*.db-wal`、`backup_*.db`（线上真实数据库，**绝不能**上传公开，且会导致更新冲突）
  - `__pycache__/`、`*.pyc`（Python 运行缓存）
- **`requirements.txt`**：记录项目依赖的 Python 包（flask、gunicorn），服务器克隆后一键安装

### 步骤（在本地电脑执行）

#### 1. 在 GitHub 创建空仓库

1. 浏览器打开 https://github.com 登录账号
2. 点击右上角 `+` → **New repository**
3. 填写：
   - Repository name：`procurement`（或任意名称）
   - 选择 **Private**（私有，推荐，代码不公开）
   - **不要**勾选 "Add a README file"（保持空仓库，避免首次推送冲突）
4. 点击 **Create repository**
5. 创建后页面会显示仓库地址，复制它，形如：
   ```
   https://github.com/你的用户名/procurement.git
   ```

#### 2. 在本地初始化 Git 并推送

在本地电脑打开 PowerShell，依次执行（把路径和仓库地址替换为你自己的）：

```powershell
# 1. 进入项目目录
cd D:\Trae\PoliceProject\Procurement

# 2. 初始化 Git 仓库（只需执行一次）
git init

# 3. 设置提交者信息（如果是第一次用 git，需要配置；换成你的信息）
git config user.name "你的名字"
git config user.email "你的邮箱@example.com"

# 4. 确认默认分支为 main
git branch -M main

# 5. 添加所有文件（.gitignore 里的文件会自动排除）
git add .

# 6. 提交（-m 后是提交说明）
git commit -m "初始化采购管理系统"

# 7. 关联远程 GitHub 仓库（用上一步复制的地址）
git remote add origin https://github.com/你的用户名/procurement.git

# 8. 推送到 GitHub
git push -u origin main
```

第 8 步会弹出 GitHub 登录窗口（或要求输入用户名和 Token），按提示登录即可。
> 如果提示输入密码：GitHub 已不支持账号密码，需要用 **Personal Access Token**（GitHub → Settings → Developer settings → Personal access tokens → 生成一个勾选 `repo` 权限的 Token，用它当密码）。更简单的方式是安装 GitHub Desktop 或使用弹出的浏览器授权登录。

> ✅ **检查点**：回到 GitHub 仓库页面刷新，能看到 `index.html`、`css/`、`js/` 等文件，说明推送成功。
>
> **确认 venv 和 .db 没有被上传**：仓库页面上**不应**出现 venv 文件夹和 .db 文件（它们已被 .gitignore 排除）。

### 以后本地代码更新了，如何推送

```powershell
cd D:\Trae\PoliceProject\Procurement
git add .
git commit -m "说明本次改了什么"
git push
```

---

## 七、服务器从 GitHub 克隆项目

### 作用

服务器通过 `git clone` 把 GitHub 上的项目下载到 `/opt/procurement`，替代手动上传文件。以后更新只需 `git pull`。

### 步骤（在服务器终端执行）

#### 1. 克隆项目到 /opt

```bash
# 1. 如果 /opt/procurement 目录已存在且为空（之前手动建过），先删除
rm -rf /opt/procurement

# 2. 从 GitHub 克隆项目（用你的仓库地址）
#    末尾的 procurement 表示克隆到 /opt/procurement 目录
git clone https://github.com/你的用户名/procurement.git /opt/procurement

# 3. 进入项目目录
cd /opt/procurement

# 4. 查看文件是否齐全
ls -la
# 应看到：index.html  css/  js/  requirements.txt  .gitignore  等
```

> 如果你的仓库是 **Private（私有）**，clone 时会要求认证：
> - 方式一：按提示输入 GitHub 用户名 + Personal Access Token
> - 方式二（推荐，免每次输密码）：配置服务器 SSH 密钥
>   ```bash
>   ssh-keygen -t ed25519 -C "server"        # 一路回车
>   cat ~/.ssh/id_ed25519.pub                # 复制输出的公钥
>   ```
>   把公钥添加到 GitHub → Settings → SSH and GPG keys → New SSH key，然后改用 SSH 地址克隆：
>   ```bash
>   git clone git@github.com:你的用户名/procurement.git /opt/procurement
>   ```

#### 2. 创建虚拟环境并安装依赖

```bash
cd /opt/procurement

# 创建 Python 虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 根据 requirements.txt 安装所有依赖（flask、gunicorn）
# 使用阿里云镜像源加速
pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
```

> **`requirements.txt` 的作用**：它记录了项目需要哪些 Python 包及版本。`pip install -r requirements.txt` 会自动安装清单里的所有包，不用手动一个个装。以后新增依赖时，把包名加进这个文件并提交到 GitHub，服务器 pull 后再执行一次该命令即可。

> ⚠️ **重要**：每次重新 SSH 登录服务器后，需先激活虚拟环境：
> ```bash
> cd /opt/procurement && source venv/bin/activate
> ```

> ✅ **检查点**：`flask --version` 能输出版本号；`ls /opt/procurement` 能看到完整项目文件 + `venv/` 目录。

---

## 八、生成服务端 app.py

### 作用

当前项目的前端是模块化版本（数据存浏览器），要让多设备数据互通，需要一个服务端 `app.py` 提供 REST API 和 SQLite 数据库。

### 步骤

> **这一步需要我来生成**：根据当前项目的模块化前端，我会生成匹配的 `app.py`（含 Flask API、SQLite 数据库初始化、WAL 并发模式、用户认证、物品管理、申请审核、采购限额、CSV 导出等全部后端接口）。
>
> 同时需要修改前端 `js/db.js`，把数据操作从本地 SQLite 改为调用服务端 API。

**当你完成前面步骤 1~7（服务器环境就绪、项目已从 GitHub 克隆）后，告诉我一声，我会：**

1. 生成云端版 `app.py`（服务端程序）
2. 修改 `js/db.js`（改为 fetch 调用服务端 API）
3. 你在本地 `git add . && git commit -m "添加服务端" && git push`
4. 服务器 `git pull` 即可获得这两个文件
5. 进入第九章测试

### 服务器上最终的目录结构

```
/opt/procurement/
├── venv/                  # Python 虚拟环境（本地生成，不入库）
├── .git/                  # Git 仓库元数据（git clone 自动生成）
├── index.html             # 前端入口
├── requirements.txt       # Python 依赖清单
├── .gitignore             # Git 忽略规则
├── css/
│   └── style.css
├── js/
│   ├── vendor/sql-wasm.js
│   ├── utils.js
│   ├── db.js              # 改为调用服务端 API
│   ├── ui.js
│   ├── app.js
│   └── pages/
│       ├── shop.js  my-requests.js  dashboard.js
│       ├── items.js  review.js  export.js  users.js
├── app.py                 # 服务端程序（Flask API）
└── procurement.db         # SQLite 数据库（运行时生成，不入库）
```

---

## 九、首次启动测试

### 作用

验证服务端程序能正常运行，数据库能正确初始化。

### 步骤

在服务器终端执行：

```bash
# 1. 确保在项目目录且虚拟环境已激活
cd /opt/procurement
source venv/bin/activate

# 2. 直接运行 Flask 开发服务器（仅测试用，生产环境用 gunicorn）
python3 app.py
```

看到类似以下输出说明启动成功：

```
 * Serving Flask app 'app'
 * Running on http://0.0.0.0:8000
 * 数据库已初始化: /opt/procurement/procurement.db
```

### 在本地电脑浏览器测试

打开浏览器，访问：

```
http://<你的IP>:8000
```

> ✅ **检查点**：
> - 能打开采购管理系统登录页
> - 用 `admin / 123456` 登录成功
> - 能看到物品列表、能提交申请、管理员能审核
>
> 测试通过后，在服务器终端按 `Ctrl + C` 停止，进入下一步配置生产运行方式。

---

## 十、配置 gunicorn 生产运行

### 作用

Flask 自带的开发服务器是单线程的，不适合生产环境。gunicorn 是专业的 Python Web 服务器，能处理并发请求、自动重启崩溃进程。

### 步骤

在服务器终端执行：

```bash
# 先确保在项目目录且虚拟环境已激活
cd /opt/procurement
source venv/bin/activate

# 测试 gunicorn 能否正常启动（Ctrl+C 停止）
gunicorn -w 1 -b 0.0.0.0:8000 app:app
```

> **参数解释**：
> - `-w 1`：使用 1 个工作进程。为什么只用 1 个？因为 SQLite 数据库不支持多进程同时写入，单进程避免写锁冲突。本系统用户量小，1 个进程完全够用。
> - `-b 0.0.0.0:8000`：监听所有网卡的 8000 端口（`0.0.0.0` 表示接受来自任意 IP 的连接）
> - `app:app`：`app` 是文件名 `app.py`（不含 .py），`:app` 是文件中 Flask 实例的变量名

> ✅ **检查点**：浏览器访问 `http://<你的IP>:8000` 能正常打开系统。测试完按 `Ctrl + C` 停止。

---

## 十一、配置 systemd 开机自启

### 作用

systemd 是 Linux 的系统服务管理器。配置后，你的应用会：
- **开机自动启动**（服务器重启后不用手动开服务）
- **崩溃自动重启**（程序意外退出后 3 秒自动恢复）
- 用 `systemctl` 命令统一管理（启动/停止/重启/查看状态）

### 步骤

#### 1. 创建服务配置文件

```bash
nano /etc/systemd/system/procurement.service
```

#### 2. 粘贴以下内容

```ini
[Unit]
Description=Procurement Management System
After=network.target

[Service]
WorkingDirectory=/opt/procurement
ExecStart=/opt/procurement/venv/bin/gunicorn -w 1 -b 127.0.0.1:8000 app:app
Restart=always
RestartSec=3
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=multi-user.target
```

> **配置说明**：
> - `Description`：服务描述，方便识别
> - `After=network.target`：确保网络服务启动后再启动本服务
> - `WorkingDirectory`：工作目录，Flask 应用读取数据库文件的相对路径基准
> - `ExecStart`：启动命令，直接用虚拟环境中的 gunicorn 全路径（避免 PATH 问题）
> - `Restart=always`：无论什么原因退出都自动重启
> - `RestartSec=3`：崩溃后等 3 秒再重启（避免频繁重启耗尽资源）
> - `Environment=PYTHONUNBUFFERED=1`：让 Python 输出不缓冲，日志实时写入
> - `WantedBy=multi-user.target`：在多用户运行级别（正常启动后）自动启动

> **nano 编辑器操作**：
> - `Ctrl+O` → 回车 → 保存
> - `Ctrl+X` → 退出

#### 3. 启用并启动服务

```bash
# 通知 systemd 重新加载配置文件（每次新建或修改 .service 文件后都要执行）
systemctl daemon-reload

# 设置开机自启 + 立即启动
systemctl enable --now procurement

# 查看运行状态
systemctl status procurement
```

> ✅ **检查点**：输出中看到 `Active: active (running)` 字样，说明服务已启动。

#### 4. 常用管理命令

```bash
systemctl restart procurement    # 重启服务（更新代码后执行）
systemctl stop procurement       # 停止服务
systemctl start procurement      # 启动服务
systemctl status procurement     # 查看状态
journalctl -u procurement -f     # 实时查看运行日志（Ctrl+C 退出查看）
journalctl -u procurement -n 100 # 查看最近 100 行日志
```

---

## 十二、安装 nginx 反向代理

### 作用

nginx 是高性能 Web 服务器，放在最外层负责接收用户请求，再转发给内部的 gunicorn。

**好处**：
1. 访问 `http://<你的IP>` 不用带 `:8000` 端口（用户更方便）
2. 以后配置 HTTPS 证书只需在 nginx 层做（不用改 Python 代码）
3. nginx 擅长处理静态文件（css/js），动态请求转发给 Flask
4. 增加安全层，隐藏内部架构

### 架构变化

```
之前：浏览器 ──► gunicorn:8000 ──► Flask

现在：浏览器 ──► nginx:80 ──► gunicorn:127.0.0.1:8000 ──► Flask
                     │
                     └─ 直接返回 css/js/html 等静态文件
```

### 步骤

#### 1. 安装 nginx

```bash
apt install -y nginx
```

#### 2. 创建站点配置文件

```bash
nano /etc/nginx/sites-available/procurement
```

#### 3. 粘贴以下配置

```nginx
server {
    listen 80;                                    # 监听 80 端口（HTTP 默认端口）
    server_name _;                                # 接受所有域名请求（绑定域名后改成你的域名）

    # 日志文件路径（方便排查问题）
    access_log /var/log/nginx/procurement_access.log;
    error_log /var/log/nginx/procurement_error.log;

    # 允许上传最大 20MB 的文件（数据库导入用）
    client_max_body_size 20m;

    # 静态文件直接由 nginx 返回（不经过 Flask，速度更快）
    location /css/ {
        alias /opt/procurement/css/;
    }
    location /js/ {
        alias /opt/procurement/js/;
    }

    # 其他所有请求转发给 Flask 处理
    location / {
        proxy_pass http://127.0.0.1:8000;          # 转发到 gunicorn
        proxy_set_header Host $host;              # 传递原始 Host 头
        proxy_set_header X-Real-IP $remote_addr;  # 传递真实客户端 IP
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

> **配置解释**：
> - `listen 80`：nginx 监听 80 端口，用户访问 `http://IP` 时默认走这里
> - `server_name _`：下划线表示匹配所有域名，绑定域名后改为你的域名
> - `location /css/` 和 `location /js/`：匹配以 /css/ 和 /js/ 开头的请求，直接从服务器本地文件返回，不经过 Flask
> - `location /`：其他所有请求（如 /api/login）转发到 Flask
> - `proxy_set_header` 系列：把客户端的真实 IP 和协议传递给 Flask，否则 Flask 只能看到 127.0.0.1

#### 4. 启用配置

```bash
# 创建软链接（符号链接），把配置文件从 available 目录链接到 enabled 目录
# nginx 只加载 sites-enabled 目录中的配置
ln -sf /etc/nginx/sites-available/procurement /etc/nginx/sites-enabled/

# 删除 nginx 默认站点配置（否则会占用 80 端口）
rm -f /etc/nginx/sites-enabled/default

# 测试配置语法是否正确
nginx -t
# 应输出：syntax is ok / test is successful

# 重新加载 nginx 配置（不中断现有连接）
systemctl reload nginx
```

> ✅ **检查点**：
> - `nginx -t` 输出 `test is successful`
> - 浏览器访问 `http://<你的IP>`（不带端口）能打开系统
> - 登录后页面样式正常（CSS 加载成功）

#### 5. 关闭 8000 端口（安全加固）

确认 nginx 代理正常后，回到第二步的安全组配置，**删除 8000 端口的规则**，只保留 80 和 443。这样外部无法直接访问 gunicorn，必须经过 nginx。

---

## 十三、域名解析与备案

### 作用

让用户通过域名（如 `pm.yourdomain.com`）访问系统，而不是难记的 IP 地址。

### 1. ICP 备案（大陆服务器必须）

> ⚠️ **如果你的服务器在中国大陆地域（如华东、华北），绑定域名对外提供网站服务必须完成 ICP 备案。**
> 如果服务器在香港或海外地域，可跳过备案。

**备案流程**：
1. 阿里云控制台 → 搜索「**ICP 备案**」→ 进入备案系统
2. 按引导填写：
   - 主体信息：个人或企业（身份证或营业执照）
   - 网站信息：域名、网站名称、用途说明
   - 上传材料：身份证照片、域名证书等
3. 提交后阿里云初审（1~2 个工作日）→ 管局审核（1~2 周）
4. 备案全程**免费**，可在备案期间先用 IP 访问调试

### 2. 域名解析（A 记录）

**作用**：把域名指向你的服务器 IP，让浏览器输入域名时能找到你的服务器。

1. 阿里云控制台 → 「**云解析 DNS**」→ 「域名解析」
2. 找到你的域名，点击进入解析设置
3. 添加解析记录：

   | 字段 | 值 | 说明 |
   |---|---|---|
   | 记录类型 | A | A 记录 = 把域名指向一个 IPv4 地址 |
   | 主机记录 | `pm` | 子域名前缀，如 pm.yourdomain.com；填 `@` 表示主域名 |
   | 解析线路 | 默认 | |
   | 记录值 | 你的服务器公网 IP | 如 `8.136.198.55` |
   | TTL | 10 分钟 | 域名解析缓存刷新时间，值越小生效越快 |

4. 保存后等待 1~10 分钟生效

> ✅ **检查点**：在本地电脑 PowerShell 执行 `ping pm.yourdomain.com`，能 ping 通你的服务器 IP 说明解析已生效。

### 3. 在 nginx 中绑定域名

```bash
nano /etc/nginx/sites-available/procurement

# 把 server_name _; 改为你的域名：
# server_name pm.yourdomain.com;

# 保存后重载 nginx
nginx -t && systemctl reload nginx
```

> ✅ **检查点**：浏览器访问 `http://pm.yourdomain.com` 能打开系统。

---

## 十四、配置 HTTPS（可选）

### 作用

- 加密浏览器与服务器之间的通信（防止密码、数据被窃听）
- 浏览器不再显示「不安全」警告
- 提升搜索引擎收录和用户信任度

### 步骤（使用 Let's Encrypt 免费证书）

```bash
# 1. 安装 certbot 和 nginx 插件
#    certbot 是 Let's Encrypt 的官方客户端，用于自动申请和续期 SSL 证书
apt install -y certbot python3-certbot-nginx

# 2. 申请证书（把域名替换为你的实际域名）
#    certbot 会自动修改 nginx 配置，添加 HTTPS 支持
certbot --nginx -d pm.yourdomain.com

# 3. 按交互提示操作：
#    - 输入你的邮箱（用于证书过期通知）
#    - 同意服务条款：输入 Y
#    - 是否分享邮箱：输入 N
#    - 选择是否重定向 HTTP 到 HTTPS：选 2（自动重定向，推荐）
```

> **Let's Encrypt 证书有效期 90 天**，certbot 会自动安装定时任务续期，无需手动操作。

> ✅ **检查点**：浏览器访问 `https://pm.yourdomain.com`，地址栏显示锁图标，说明 HTTPS 已生效。

---

## 十五、日常运维

### 1. 数据库自动备份（重要！）

**作用**：防止服务器故障、误操作导致数据丢失。每天凌晨自动备份一份。

```bash
# 编辑定时任务
crontab -e

# 在文件末尾添加以下两行（保存退出即生效）：
# 每天凌晨 2:00 备份数据库
0 2 * * * cp /opt/procurement/procurement.db /opt/procurement/backup_$(date +\%Y\%m\%d).db
# 每周日凌晨 3:00 清理 30 天前的旧备份
0 3 * * 0 find /opt/procurement -name "backup_*.db" -mtime +30 -delete
```

> **crontab 格式说明**：`分 时 日 月 周 命令`
> - `0 2 * * *` = 每天 2:00 执行
> - `0 3 * * 0` = 每周日 3:00 执行

建议每月用 WinSCP 把最近的 `backup_*.db` 文件下载到本地电脑一份，双重保险。

### 2. 通过 GitHub 更新代码（推荐方式）

**作用**：项目代码改了之后，本地推送到 GitHub，服务器一条 `git pull` 就能更新，无需手动传文件。

#### 标准更新流程

**第一步：本地电脑推送改动**（在本地 PowerShell 执行）

```powershell
cd D:\Trae\PoliceProject\Procurement

# 查看改了哪些文件（确认无误）
git status

# 添加所有改动并提交
git add .
git commit -m "说明本次改了什么，例如：增加采购限额功能"

# 推送到 GitHub
git push
```

**第二步：服务器拉取更新并重启**（在服务器终端执行）

```bash
cd /opt/procurement

# 拉取 GitHub 上的最新代码
git pull

# 如果 requirements.txt 有变化（新增了依赖包），重新安装依赖
source venv/bin/activate
pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/

# 重启 Flask 服务，使新代码生效
systemctl restart procurement

# （如果改了 nginx 配置才需要）重载 nginx
# nginx -t && systemctl reload nginx
```

> ✅ 更新后浏览器强制刷新（Ctrl+F5）即可看到新功能。

#### 常见问题处理

**情况 1：`git pull` 提示 "Your local changes would be overwritten"**

这通常是因为你在服务器上手动改过代码（不建议直接在服务器改）。处理：

```bash
# 方案 A：服务器上的改动不要了，完全以 GitHub 为准（会丢弃服务器本地修改）
git checkout .
git pull

# 方案 B：想保留服务器改动并合并（较少用到）
git stash
git pull
git stash pop
```

> 注意：`procurement.db`、`venv/` 已被 .gitignore 排除，`git pull` **不会**影响线上数据库数据，可放心更新。

**情况 2：更新后页面报错**

```bash
# 查看错误日志定位问题
journalctl -u procurement -n 50

# 如果新代码有问题想回退到上一个版本
cd /opt/procurement
git log --oneline -5          # 查看最近 5 次提交，找到上一个版本的编号
git checkout 上一个版本编号     # 回退代码
systemctl restart procurement
```

### 3. 查看日志

```bash
# 查看 Flask 应用日志（最近 100 行）
journalctl -u procurement -n 100

# 实时查看 Flask 日志（滚动输出，Ctrl+C 退出）
journalctl -u procurement -f

# 查看 nginx 访问日志（谁访问了你的网站）
tail -f /var/log/nginx/procurement_access.log

# 查看 nginx 错误日志
tail -f /var/log/nginx/procurement_error.log
```

### 4. 安全建议

- **部署后第一时间修改默认密码**：登录系统 → 用户管理 → 修改 admin 密码
- **SSH 密钥登录**：更安全的做法是改用 SSH 密钥登录并禁用密码登录（防止暴力破解）
- **定期更新系统**：每月执行一次 `apt update && apt upgrade -y`
- **不要在服务器上存放无关文件**：减少安全风险

---

## 十六、常见问题排查

| 现象 | 排查步骤 |
|---|---|
| 浏览器完全打不开页面 | ① 安全组是否放行 80 端口（见第二步）<br>② `systemctl status procurement` 是否 active<br>③ `systemctl status nginx` 是否 active<br>④ 服务器上 `curl 127.0.0.1:8000` 是否有响应 |
| 提示 502 Bad Gateway | gunicorn 进程没起来：`journalctl -u procurement -n 50` 看具体报错。常见原因：app.py 上传不完整、依赖未安装、数据库权限问题 |
| 页面打开但样式丢失 | nginx 静态文件配置有误：检查 `/etc/nginx/sites-available/procurement` 中 `location /css/` 和 `location /js/` 路径是否正确 |
| IP 能访问，域名不行 | ① 域名解析未生效：`ping 域名` 检查<br>② 大陆服务器未备案：完成 ICP 备案<br>③ nginx 中 server_name 未改为域名 |
| 访问速度慢 | ① 检查带宽是否跑满（控制台监控）<br>② 大陆服务器配未备案域名会被阻断<br>③ 检查服务器 CPU/内存使用率 |
| 数据丢了 | procurement.db 在 `/opt/procurement/` 目录，重装系统或误删目录会清空。务必按第十五步配置每日备份 |
| 登录后操作报错 | 检查 `journalctl -u procurement -n 50` 日志，常见是数据库表结构不匹配（旧版数据库需升级） |
| 忘记 root 密码 | ECS 控制台 → 实例 → 「重置实例密码」→ 重启实例生效 |
| HTTPS 证书过期 | `certbot renew` 手动续期，或检查 `systemctl status certbot.timer` 是否正常 |
| `git pull` 拉取失败 | ① 提示 local changes：见第十五章「情况 1」，执行 `git checkout .` 后再 pull<br>② 私有仓库认证失败：按第七章配置 SSH 密钥，或重新输入 Token<br>③ 拉取后依赖缺失：`source venv/bin/activate && pip install -r requirements.txt` |

---

## 十七、下一步

按上述步骤完成第一~七步（服务器环境就绪、项目已从 GitHub 克隆到 `/opt/procurement`）后，告诉我一声，我会：

1. **生成云端版 `app.py`**：Flask REST API 服务端，包含所有后端接口（用户认证、物品管理、采购申请、审核、限额管理、CSV 导出等），开启 WAL 并发模式，数据存储在服务器端 SQLite 文件 `procurement.db` 中

2. **修改 `js/db.js`**：把前端数据操作从本地浏览器 SQLite 改为 `fetch()` 调用服务端 API，前端其他文件（页面逻辑）基本不变

3. 交付后你只需三步上线：
   ```powershell
   # 本地：提交并推送
   git add . ; git commit -m "添加服务端" ; git push
   ```
   ```bash
   # 服务器：拉取并重启
   cd /opt/procurement && git pull && systemctl restart procurement
   ```
   然后按第九步测试即可。

**上线后效果**：所有设备（电脑/手机/平板）访问 `http://你的域名`，数据实时互通——普通用户在 A 设备提交申请，管理员在 B 设备立刻能看到。以后每次更新都是「本地 push → 服务器 pull → 重启」，一条命令搞定。
